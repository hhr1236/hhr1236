# 完整使用指南 / Complete User Guide

## 🎯 目标

帮助你准确确定船舶领域的长轴（主轴），特别是在浅水效应下。

## 📁 文件导航

### 核心文件

| 文件名 | 用途 | 适合人群 |
|--------|------|----------|
| `解决方案总结.md` | 快速了解问题和解决方案 | ⭐⭐⭐ 所有人先看这个 |
| `practical_example.py` | 运行对比示例，看效果 | ⭐⭐⭐ 推荐先运行 |
| `ship_domain_analysis.py` | 完整的分析工具 | 需要高级功能时使用 |
| `长轴确定方法论.md` | 详细的理论和方法 | 想深入理解的人 |
| `船舶领域长轴确定方案.md` | 快速入门指南 | 需要快速上手的人 |
| `INSTALL.md` | 安装和配置 | 遇到技术问题时查看 |

### 辅助文件

| 文件名 | 用途 |
|--------|------|
| `create_diagram.py` | 生成方法对比图 |
| `requirements.txt` | Python依赖包列表 |
| `README.md` | 项目概览 |

## 🚀 快速开始（10分钟）

### 第1步：阅读问题分析（2分钟）

打开 `解决方案总结.md`，快速浏览：
- 你的原代码存在什么问题
- 我们提供什么解决方案
- 预期能改进多少

### 第2步：安装依赖（3分钟）

```bash
# 安装Python包
pip install numpy matplotlib scipy

# 验证安装
python -c "import numpy; import matplotlib; import scipy; print('安装成功！')"
```

### 第3步：运行示例（5分钟）

```bash
# 运行对比示例
python practical_example.py
```

会生成两张图：
- `method_comparison.png` - 原方法 vs 改进方法
- `sensitivity_analysis.png` - 参数敏感性分析

**查看这两张图，理解改进效果！**

## 📚 学习路径

### 路径A：快速应用（1-2小时）⭐ 推荐

适合：想快速改进代码的人

1. ✅ 阅读 `解决方案总结.md`（10分钟）
2. ✅ 运行 `python practical_example.py`（10分钟）
3. ✅ 按照"方案A：最小改动"修改你的代码（30分钟）
4. ✅ 运行你的改进代码，对比结果（30分钟）

### 路径B：深入理解（半天）

适合：想彻底理解方法的人

1. ✅ 阅读 `解决方案总结.md`（10分钟）
2. ✅ 阅读 `长轴确定方法论.md`（30分钟）
3. ✅ 研究 `ship_domain_analysis.py` 源码（1小时）
4. ✅ 运行所有示例，理解每个方法（1小时）

### 路径C：完整实施（1-2天）

适合：需要完整解决方案的人

1. ✅ 完成路径B的所有内容
2. ✅ 使用 `ShipDomainAnalyzer` 类集成到项目
3. ✅ 进行多工况分析和参数敏感性分析
4. ✅ 验证结果，编写报告

## 🔧 如何改进你的代码

### 方案1：最小改动（推荐给初学者）

**优点**：改动小，风险低，容易理解  
**缺点**：功能相对简单

在你的原代码中做以下修改：

```python
# ===== 原代码（需要修改的部分） =====
# footprint_right = generate_maneuvering_footprint(rudder_angle_deg=15.0, ...)
# footprint_left = generate_maneuvering_footprint(rudder_angle_deg=-15.0, ...)

# ===== 改进代码（替换为这个） =====
# 定义多个测试工况
test_angles = [15, 20, 25, 30]  # 从小到大测试
test_speeds = [6.0]  # 如果想测试多个速度，可以改为 [5.0, 6.0, 7.0]

all_footprints_combined = []

for angle in test_angles:
    for speed in test_speeds:
        print(f"测试工况: 舵角={angle}°, 速度={speed}m/s")
        
        # 右转
        fp_r = generate_maneuvering_footprint(
            rudder_angle_deg=angle, 
            target_lateral_dist=SAFE_LATERAL_DISTANCE
            # 注意：你需要修改你的函数支持传入速度参数
            # 或者在函数内部修改 initial_motion 的速度值
        )
        
        # 左转
        fp_l = generate_maneuvering_footprint(
            rudder_angle_deg=-angle,
            target_lateral_dist=SAFE_LATERAL_DISTANCE
        )
        
        all_footprints_combined.extend(fp_r)
        all_footprints_combined.extend(fp_l)

# 后面的凸包计算代码不变
master_point_cloud = np.array(all_footprints_combined)
hull = ConvexHull(master_point_cloud)
hull_vertices = master_point_cloud[hull.vertices]

L_footprint = np.max(hull_vertices[:, 1]) - np.min(hull_vertices[:, 1])
B_footprint = np.max(hull_vertices[:, 0]) - np.min(hull_vertices[:, 0])

# ===== 重要：添加安全系数 =====
SAFETY_FACTOR = 1.20  # 浅水条件建议20%
L_final = L_footprint * SAFETY_FACTOR
B_final = max(B_footprint, REQUIRED_DOMAIN_WIDTH) * SAFETY_FACTOR

print(f"\n自然长轴: {L_footprint:.2f}m")
print(f"含安全系数长轴: {L_final:.2f}m ({L_final/SHIP_LENGTH:.2f} 倍船长)")
```

### 方案2：使用分析工具（推荐给进阶用户）

**优点**：功能强大，分析全面  
**缺点**：需要学习新的API

```python
from ship_domain_analysis import ShipDomainAnalyzer

# 定义你的浅水模型适配函数
def my_shallow_model(rudder_angle_rad, motion):
    """
    适配器函数：将你的MMG模型包装成统一接口
    """
    ship_data_info = list_ship_data()[1]
    ship_data_with_rudder = list(ship_data_info)
    ship_data_with_rudder[-1] = rudder_angle_rad
    
    model = MMG_Model_new1(ship_data_with_rudder, motion, 198.5, 23)
    return model.get_next_moment()

# 创建分析器
analyzer = ShipDomainAnalyzer(
    ship_length=SHIP_LENGTH,
    ship_width=SHIP_WIDTH,
    shallow_model_func=my_shallow_model
)

# 定义测试工况
test_cases = [
    {'rudder_angle': 15, 'speed': 6.0, 'lateral_dist': 150},
    {'rudder_angle': 20, 'speed': 6.5, 'lateral_dist': 150},
    {'rudder_angle': 25, 'speed': 7.0, 'lateral_dist': 150},
    {'rudder_angle': 30, 'speed': 7.5, 'lateral_dist': 150},
]

# 执行多工况分析
analyzer.multi_scenario_analysis(test_cases)

# 获取推荐参数
analysis = analyzer.recommend_domain_parameters()

# 使用结果
L_final = analysis['L_with_safety']
B_final = analysis['B_with_safety']

print(f"推荐长轴: {L_final:.2f}m ({L_final/SHIP_LENGTH:.2f} 倍船长)")
print(f"推荐短轴: {B_final:.2f}m ({B_final/SHIP_WIDTH:.2f} 倍船宽)")

# 可视化
analyzer.visualize_comprehensive(analysis)
```

## 📊 如何验证结果

### 检查清单

1. **合理性检查**
   ```python
   # 对于225m船长的船舶，长轴应该在：
   # 开阔水域: 4-6倍船长 (900-1350m)
   # 受限水域: 6-8倍船长 (1350-1800m)
   # 浅水区域: 8-12倍船长 (1800-2700m)
   
   L_ratio = L_final / SHIP_LENGTH
   if 4 <= L_ratio <= 12:
       print(f"✅ 长轴合理：{L_ratio:.2f}倍船长")
   else:
       print(f"⚠️  长轴可能不合理：{L_ratio:.2f}倍船长，请检查")
   ```

2. **对比检查**
   ```python
   # 对比原方法和改进方法
   L_original = 700  # 你原方法的结果
   L_improved = 1000  # 改进方法的结果
   improvement = (L_improved - L_original) / L_original * 100
   
   print(f"改进幅度: +{improvement:.1f}%")
   # 预期：20-40%的增加是正常的
   ```

3. **极限测试**
   ```python
   # 测试极端工况（大舵角35°）
   fp_extreme = generate_maneuvering_footprint(35, ...)
   L_extreme = calculate_length(fp_extreme)
   
   if L_extreme <= L_final:
       print("✅ 领域能够包络极端工况")
   else:
       print("⚠️  需要调整领域尺寸")
   ```

## ❓ 常见问题速查

| 问题 | 答案 | 详见 |
|------|------|------|
| 为什么改进后长轴更大？ | 因为考虑了更多工况+安全系数 | `解决方案总结.md` Q1 |
| 安全系数应该取多少？ | 浅水条件：20-25% | `长轴确定方法论.md` |
| 长轴多少才合理？ | 4-12倍船长（看水域类型） | `解决方案总结.md` Q2 |
| 应该用哪种方法？ | 多工况包络法（方法1） | `解决方案总结.md` Q3 |
| 如何验证准确性？ | 对比法+极限法+专家法 | 本文"如何验证结果" |
| 代码运行出错怎么办？ | 检查依赖包安装 | `INSTALL.md` |

## 🎓 推荐阅读顺序

### 第一天：理解问题

1. 📄 `解决方案总结.md`（必读）
2. 🔬 运行 `practical_example.py`（必做）
3. 📸 查看生成的对比图（必看）

### 第二天：学习方法

1. 📚 `长轴确定方法论.md`（推荐读）
2. 📘 `船舶领域长轴确定方案.md`（快速参考）
3. 💻 研究 `ship_domain_analysis.py`（可选）

### 第三天：实际应用

1. 🔧 修改你的代码（按照方案1或方案2）
2. ✅ 验证结果（使用检查清单）
3. 📊 编写实验报告

## 🆘 获取帮助

### 自助排查

1. **代码不运行？**
   - 检查：Python版本（需要3.8+）
   - 检查：依赖包（numpy, matplotlib, scipy）
   - 参考：`INSTALL.md`

2. **中文显示乱码？**
   - 检查：字体配置
   - 参考：`INSTALL.md` 常见问题

3. **结果不合理？**
   - 检查：输入参数是否正确
   - 检查：MMG模型是否正常工作
   - 参考：本文"如何验证结果"

### 进一步学习

如果想更深入了解船舶领域理论，推荐阅读：

1. Goodwin, E. M. (1975). A statistical study of ship domains.
2. Fujii, Y., & Tanaka, K. (1971). Traffic capacity.
3. Davis, P. V., et al. (1980). A computer simulation of marine traffic.

## 📝 总结

### 关键要点

1. ✅ **必须测试多个工况**（不能只测一个舵角和速度）
2. ✅ **必须应用安全系数**（浅水条件建议20-25%）
3. ✅ **必须验证结果**（检查是否在4-12倍船长范围内）
4. ✅ **必须取最大包络**（所有工况的并集）

### 不要做的事

1. ❌ 不要只测试单一工况
2. ❌ 不要忽略安全系数
3. ❌ 不要跳过结果验证
4. ❌ 不要低估浅水效应

### 下一步

1. 🎯 运行 `practical_example.py` 看效果
2. 📝 阅读 `解决方案总结.md` 理解方案
3. 🔧 按照"方案1"修改你的代码
4. ✅ 验证结果并记录

---

**祝你实验成功！** 🚢✨

有问题随时查阅本指南或相关文档。
