# 船舶领域长轴确定方法论

## 问题分析

您的代码中存在的核心问题：

1. **单一工况问题**：仅使用舵角±15°和单一航速进行仿真，无法覆盖所有可能的操纵场景
2. **直接取值问题**：直接使用`L_footprint = max(y) - min(y)`，没有考虑安全裕度
3. **缺乏验证**：没有对比不同参数组合下的结果，无法确认长轴是否准确
4. **浅水效应复杂性**：浅水效应对不同操纵阶段的影响不同，单次仿真难以全面捕捉

## 改进方案

### 方案一：多工况包络法 ⭐⭐⭐⭐⭐（强烈推荐）

**原理**：通过测试多种可能的操纵场景，取所有场景的包络作为最终领域。

**实施步骤**：

```python
# 1. 设计测试工况矩阵
test_matrix = [
    # 格式：(舵角, 航速, 目标横距)
    (10, 5.0, 150),   # 小舵角，低速
    (15, 6.0, 150),   # 中舵角，中速（你当前使用的）
    (20, 6.5, 150),   # 大舵角，中速
    (25, 7.0, 150),   # 大舵角，高速
    (15, 5.0, 200),   # 中舵角，低速，大横距
    (20, 6.0, 200),   # 大舵角，中速，大横距
]

# 2. 对每个工况进行左右对称仿真
all_footprints = []
for rudder, speed, lateral in test_matrix:
    fp_right = simulate(rudder, speed, lateral)
    fp_left = simulate(-rudder, speed, lateral)
    all_footprints.extend(fp_right)
    all_footprints.extend(fp_left)

# 3. 计算总包络
hull = ConvexHull(all_footprints)
L_final = calculate_length(hull)
```

**优点**：
- ✅ 考虑了多种操纵场景
- ✅ 安全性高，不会遗漏极端情况
- ✅ 适用于浅水效应复杂的情况

**缺点**：
- ⚠️ 计算量较大
- ⚠️ 可能过于保守

### 方案二：参数敏感性分析法 ⭐⭐⭐⭐

**原理**：系统地改变关键参数，找出对长轴影响最大的因素。

**实施步骤**：

```python
# 1. 测试舵角的影响
rudder_angles = [5, 10, 15, 20, 25, 30, 35]
L_vs_rudder = []
for rudder in rudder_angles:
    L = calculate_length_for_rudder(rudder)
    L_vs_rudder.append((rudder, L))

# 2. 测试航速的影响
speeds = [4.0, 5.0, 6.0, 7.0, 8.0]
L_vs_speed = []
for speed in speeds:
    L = calculate_length_for_speed(speed)
    L_vs_speed.append((speed, L))

# 3. 分析结果
# 绘制L vs 舵角曲线
# 绘制L vs 航速曲线
# 找出主导参数

# 4. 基于主导参数选择设计工况
```

**优点**：
- ✅ 能理解长轴的物理本质
- ✅ 找到关键影响因素
- ✅ 可以优化仿真次数

**应用示例**：
```
假设分析发现：
- 舵角从15°增加到25°，长轴增加30%
- 航速从6m/s增加到8m/s，长轴仅增加5%

结论：舵角是主导因素，应重点考虑大舵角工况
```

### 方案三：分阶段贡献分析法 ⭐⭐⭐⭐

**原理**：将操纵过程分为不同阶段，分析各阶段对长轴的贡献。

**操纵过程分解**：

1. **起始阶段**（0-50m）：船舶开始转向，主要是前向运动
2. **转向阶段**（50-200m）：船舶大幅度转向，横向和纵向位移都很大
3. **稳定阶段**（200m-终点）：接近目标横距，主要调整姿态

**分析方法**：

```python
# 记录每个阶段的边界点
phase1_points = []  # y in [0, 50]
phase2_points = []  # y in [50, 200]
phase3_points = []  # y > 200

for point in footprint:
    y = point[1]
    if y < 50:
        phase1_points.append(point)
    elif y < 200:
        phase2_points.append(point)
    else:
        phase3_points.append(point)

# 分析各阶段的纵向范围
L_phase1 = max_y(phase1_points) - min_y(phase1_points)
L_phase2 = max_y(phase2_points) - min_y(phase2_points)
L_phase3 = max_y(phase3_points) - min_y(phase3_points)

print(f"第一阶段贡献: {L_phase1} ({L_phase1/L_total*100:.1f}%)")
print(f"第二阶段贡献: {L_phase2} ({L_phase2/L_total*100:.1f}%)")
print(f"第三阶段贡献: {L_phase3} ({L_phase3/L_total*100:.1f}%)")
```

**优点**：
- ✅ 理解长轴的形成机理
- ✅ 可以针对性优化
- ✅ 便于向他人解释

### 方案四：统计置信区间法 ⭐⭐⭐

**原理**：多次重复仿真，使用统计方法确定长轴。

**实施步骤**：

```python
# 1. 多次仿真（添加小扰动模拟不确定性）
n_runs = 20
L_samples = []

for i in range(n_runs):
    # 添加小扰动（如±5%的初始速度扰动）
    speed_perturb = 6.0 * (1 + np.random.uniform(-0.05, 0.05))
    rudder_perturb = 15.0 * (1 + np.random.uniform(-0.03, 0.03))
    
    footprint = simulate(rudder_perturb, speed_perturb, 150)
    L = calculate_length(footprint)
    L_samples.append(L)

# 2. 统计分析
L_mean = np.mean(L_samples)
L_std = np.std(L_samples)
L_99_percentile = np.percentile(L_samples, 99)

# 3. 使用99%置信上界作为设计值
L_final = L_99_percentile
```

**优点**：
- ✅ 考虑不确定性
- ✅ 有统计学依据
- ✅ 可调整置信水平

### 方案五：经验安全系数法 ⭐⭐⭐

**原理**：在自然尺寸基础上乘以安全系数。

```python
# 当前你的代码
L_natural = max(y) - min(y)

# 改进：应用安全系数
SAFETY_FACTOR = 1.15  # 15%安全裕度（参考IMO规范）

L_final = L_natural * SAFETY_FACTOR
```

**常用安全系数参考**：
- 开阔水域：1.10 - 1.15
- 受限水域：1.15 - 1.20
- 浅水区域：1.20 - 1.30
- 高密度交通区：1.30 - 1.50

## 推荐的实验流程

### 第一步：快速验证（1-2小时）

```python
# 使用你当前的代码作为基准
baseline_L = run_single_scenario(rudder=15, speed=6.0)

# 测试几个极端工况
extreme_scenarios = [
    (25, 7.0, 150),  # 大舵角高速
    (10, 5.0, 200),  # 小舵角低速大横距
]

for scenario in extreme_scenarios:
    L = run_scenario(*scenario)
    print(f"工况 {scenario}: L = {L:.2f}m (vs 基准 {baseline_L:.2f}m)")
```

**预期结果**：如果极端工况的L值显著大于基准值（>10%），说明你当前的长轴确实偏小。

### 第二步：系统分析（半天）

```python
# 实施方案一：多工况包络法
test_matrix = [
    (10, 5.0, 150), (15, 5.0, 150), (20, 5.0, 150),
    (10, 6.0, 150), (15, 6.0, 150), (20, 6.0, 150),
    (10, 7.0, 150), (15, 7.0, 150), (20, 7.0, 150),
]

results = multi_scenario_analysis(test_matrix)
L_recommended = results['L_with_safety']
```

### 第三步：验证和可视化（半天）

```python
# 绘制综合图表
fig, axes = plt.subplots(2, 2, figsize=(16, 12))

# 子图1: 所有工况的足迹叠加
# 子图2: 参数敏感性曲线
# 子图3: 统计分布直方图
# 子图4: 最终领域推荐

plt.savefig('domain_analysis_results.png')
```

## 针对浅水效应的特殊考虑

浅水效应会导致：

1. **回转直径增大**：船舶转向性能下降，需要更大的空间完成避让
2. **漂角增大**：侧向力增加，纵向位移增大
3. **操纵响应延迟**：从舵角施加到船舶响应有时间延迟

**建议**：

```python
# 对于浅水条件，建议的测试矩阵应该更宽泛
shallow_water_test_matrix = [
    # 基础工况
    (15, 6.0, 150),
    # 更大舵角（因为回转性能下降）
    (25, 6.0, 150),
    (30, 6.0, 150),
    # 更大横距（因为可能需要更早避让）
    (20, 6.0, 200),
    (25, 6.0, 200),
]

# 安全系数也应该更大
SHALLOW_WATER_SAFETY_FACTOR = 1.25  # 25%裕度
```

## 代码实现示例

我已经在`ship_domain_analysis.py`中提供了完整的实现，包括：

1. `ShipDomainAnalyzer`类：封装了所有分析方法
2. `multi_scenario_analysis()`：多工况分析
3. `sensitivity_analysis()`：参数敏感性分析
4. `analyze_domain_dimensions()`：尺寸分析（含安全系数）
5. `visualize_comprehensive()`：综合可视化

**使用方法**：

```python
from ship_domain_analysis import ShipDomainAnalyzer

# 1. 创建分析器（需要替换为你的实际模型）
analyzer = ShipDomainAnalyzer(
    ship_length=225.0,
    ship_width=225.0/8,
    shallow_model_func=your_mmg_model  # 传入你的MMG模型
)

# 2. 多工况分析
test_cases = [
    {'rudder_angle': 15, 'speed': 6.0, 'lateral_dist': 150},
    {'rudder_angle': 20, 'speed': 6.5, 'lateral_dist': 150},
    {'rudder_angle': 25, 'speed': 7.0, 'lateral_dist': 150},
]
analyzer.multi_scenario_analysis(test_cases)

# 3. 获得推荐参数
analysis = analyzer.recommend_domain_parameters()
print(f"推荐长轴: {analysis['L_with_safety']:.2f}m")

# 4. 可视化
analyzer.visualize_comprehensive(analysis)
```

## 常见问题解答

### Q1: 为什么不能直接用单一工况的结果？

**A**: 因为：
1. 实际操纵中可能遇到多种场景
2. 不同舵角和航速组合会导致不同的足迹
3. 浅水效应在不同操纵状态下表现不同
4. 单一工况可能无法覆盖最坏情况

### Q2: 安全系数应该取多少？

**A**: 建议：
- 正常水深：1.15（15%）
- 浅水效应：1.20-1.25（20-25%）
- 极浅水：1.30（30%）

也可以通过统计方法确定（如99%置信区间）。

### Q3: 长轴应该对称吗？

**A**: 不一定。实际上船舶领域通常是前后不对称的：
- 前方延伸较长（因为船舶向前运动）
- 后方延伸较短

你的代码中已经计算了`center_offset_y`，这是正确的做法。

### Q4: 如何验证长轴的准确性？

**A**: 
1. **对比法**：与文献中的船舶领域模型对比
2. **极限法**：测试极端工况，确保领域能包络
3. **实船法**：如有条件，用实船数据验证
4. **专家法**：请有经验的船长评估是否合理

### Q5: 我的长轴是多少才算合理？

**A**: 一般经验值（相对于船长Ls）：
- 开阔水域：4-6 Ls
- 受限水域：6-8 Ls
- 浅水区域：8-12 Ls

你的船长225m，如果长轴在900-1800m之间都是合理的，具体取决于水深和操纵性能。

## 总结

**最佳实践流程**：

1. ✅ 使用多工况包络法作为主要方法
2. ✅ 进行参数敏感性分析作为验证
3. ✅ 应用15-25%的安全系数
4. ✅ 用可视化图表辅助理解
5. ✅ 对比文献和经验值检查合理性

**关键原则**：

- 宁可保守（偏大）也不要激进（偏小）
- 考虑多种操纵场景，不要只测单一工况
- 浅水效应需要特殊对待，安全系数要更大
- 长轴应该基于物理仿真+安全裕度，而非纯粹的经验公式

祝实验顺利！如有问题，可以参考`ship_domain_analysis.py`中的代码实现。
