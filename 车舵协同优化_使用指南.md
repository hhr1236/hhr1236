# 车舵协同避让决策优化 - 使用指南

## 概述

这是对您现有避让决策系统的扩展，从**仅优化转向角度**升级到**车舵协同优化**（同时优化转向角度和速度档位）。

## 核心改进

### 原系统
- **决策变量**：仅转向角度 (5° - 35°)
- **优化方法**：帕累托前沿 + 加权评分
- **输出**：最优转向角度

### 新系统
- **决策变量**：(转向角度, 速度档位) 组合
  - 角度范围：5° - 35°
  - 速度档位：前进三(85/60)、前进二(70/60)、前进一(48/60)
- **优化方法**：多目标帕累托优化
- **输出**：最优的车舵协同方案

## 主要优势

1. **更丰富的决策空间**
   - 从31种方案（仅角度）→ 93种方案（角度×速度）
   - 能找到更优的权衡解

2. **多维度优化目标**
   - DCPA（安全距离）
   - 风险度（航道边界风险）
   - 避让时间（操作效率）
   - 燃油经济性（运营成本）
   - 操作成本（转向角度大小）

3. **灵活的决策权重**
   - 可根据不同场景调整权重
   - 紧急避让：增加DCPA权重
   - 经济航行：增加燃油权重

## 如何集成到您的代码

### 第1步：修改速度控制

在您的代码中找到设置速度的位置，添加速度档位控制：

```python
# 原代码（固定速度）
list_own_ship_sim = typed.List(list_ship_data()[0])

# 修改为（可变速度）
def set_speed_gear(ship_params, gear_idx):
    """
    设置速度档位
    gear_idx: 0=前进三(85/60), 1=前进二(70/60), 2=前进一(48/60)
    """
    GEAR_VALUES = {
        0: 85.0 / 60.0,  # 前进三
        1: 70.0 / 60.0,  # 前进二
        2: 48.0 / 60.0,  # 前进一
    }
    
    ship_params[11] = GEAR_VALUES[gear_idx]  # 关键：修改索引11的值
    return ship_params

# 使用示例
list_own_ship_sim = typed.List(list_ship_data()[0])
list_own_ship_sim = set_speed_gear(list_own_ship_sim, gear_idx=1)  # 设置为前进二
```

### 第2步：替换评估函数

将Demo中的`evaluate_rudder_speed_combination`函数改为调用您的实际仿真：

```python
def evaluate_rudder_speed_combination(
    angle, speed_gear_idx, model,
    list_own, list_tar, list_own_ship_initial,
    deep, bank_p1, bank_p2, RR1, RR2,
    U_threshold, original_course
):
    """实际集成版本"""
    
    # 1. 设置速度档位
    list_own_ship_sim = typed.List([val for val in list_own_ship_initial])
    list_own_ship_sim = set_speed_gear(list_own_ship_sim, speed_gear_idx)
    
    # 2. 设置转向角度
    mbhx_own = (list_own[2] * 180 / math.pi + angle) * math.pi / 180
    
    # 3. 复制状态（避免修改原始数据）
    list_own_copy = typed.List([val for val in list_own])
    list_tar_copy = typed.List([val for val in list_tar])
    
    # 4. 计算航道边界
    m_right, b_right = line_equation(bank_p1, bank_p2)
    
    # 5. 初始化性能指标
    max_risk = 0.0
    final_dcpa = -1.0
    status = 0
    steps = 0
    
    # 6. 仿真循环（与您原代码相同的逻辑）
    for j in range(1, 801):
        steps = j
        
        # 计算离岸距离
        dist_to_bank = point_to_line_distance(
            list_own_copy[0], list_own_copy[1], m_right, b_right
        )
        
        # PID控制舵角
        delta = pid_duojiao(list_own_copy, mbhx_own, 1200, 105, 0.1, dist_to_bank, deep)
        list_own_ship_sim[-1] = delta
        
        # 更新状态
        list_own_copy = compute_next_moment(
            list_own_ship_sim, list_own_copy, dist_to_bank, deep
        )
        list_tar_copy = get_tar_position(list_tar_copy)
        
        # 计算当前风险度
        current_risk = calculate_comprehensive_risk_value(
            list_own_copy, dist_to_bank, bank_p1, bank_p2, RR1, RR2
        )
        max_risk = max(max_risk, current_risk)
        
        # 检查约束
        if current_risk > U_threshold:
            status = -2  # 超出风险阈值
            break
        
        # 检查碰撞
        L, B = 225.0, 32.5
        a, b = (1.3 * L, 2 * B) if model == 'meet' else (1.3 * L, 2 * B)
        x_inv = list_own_copy[0] + 0.25 * L * math.sin(list_own_copy[2])
        y_inv = list_own_copy[1] + 0.25 * L * math.cos(list_own_copy[2])
        
        if ((list_tar_copy[0] - x_inv) ** 2 / a ** 2 + 
            (list_tar_copy[1] - y_inv) ** 2 / b ** 2 - 1.0) < 0:
            status = -1  # 碰撞
            break
        
        # 检查是否可以复航
        all_targets = typed.List()
        all_targets.append(list_tar_copy)
        
        if is_safe_to_resume_course_numba(
            list_own_copy, all_targets, list_own_ship_sim,
            original_course, deep, m_right, b_right
        ):
            status = 1  # 成功
            _tcpa, final_dcpa = CPA(list_own_copy, list_tar_copy)
            break
    
    # 7. 返回结果
    gear_name, gear_value = GEAR_INDEX[speed_gear_idx]
    
    return {
        'angle': angle,
        'speed_gear': gear_name,
        'speed_value': gear_value,
        'status': status,
        'dcpa': final_dcpa if status == 1 else -1,
        'max_risk': max_risk,
        'steps': steps,
        'fuel_factor': gear_value  # 可根据实际情况调整
    }
```

### 第3步：运行优化

```python
# 在您的主程序中
if __name__ == '__main__':
    # ... 您的初始化代码 ...
    
    # 步骤1：评估所有车舵组合
    all_results, successful_results = optimize_rudder_speed_coordination(
        angle_range=(5, 36),
        speed_gears=(0, 1, 2),  # 评估所有三个档位
        model='meet',
        list_own=list_own_initial,
        list_tar=list_tar_initial,
        list_own_ship_initial=list_own_ship_initial_template,
        deep=deep_dummy,
        bank_p1=p1,
        bank_p2=p2,
        RR1=RR1,
        RR2=RR2,
        U_threshold=RISK_THRESHOLD,
        original_course=ORIGINAL_COURSE
    )
    
    # 步骤2：帕累托前沿筛选
    pareto_front = find_pareto_front_multi_objective(successful_results)
    
    # 步骤3：加权决策
    decision_weights = {
        'dcpa': 0.30,   # 安全距离权重
        'risk': 0.30,   # 风险度权重
        'steps': 0.20,  # 避让时间权重
        'fuel': 0.10,   # 燃油权重
        'angle': 0.10   # 操作成本权重
    }
    
    best_solution, best_score = select_best_solution(pareto_front, decision_weights)
    
    # 步骤4：输出结果
    if best_solution:
        print(f"\n最优车舵协同方案：")
        print(f"  转向角度: {best_solution['angle']}°")
        print(f"  速度档位: {best_solution['speed_gear']}")
        print(f"  预期DCPA: {best_solution['dcpa']:.1f} m")
        print(f"  最大风险度: {best_solution['max_risk']:.4f}")
    
    # 步骤5：可视化
    visualize_rudder_speed_space(all_results, pareto_front, best_solution)
```

## 权重调整建议

根据不同场景调整决策权重：

### 场景1：紧急避让（优先安全）
```python
weights = {
    'dcpa': 0.50,   # 增加安全距离权重
    'risk': 0.30,
    'steps': 0.10,
    'fuel': 0.05,
    'angle': 0.05
}
```

### 场景2：经济航行（优先节能）
```python
weights = {
    'dcpa': 0.20,
    'risk': 0.20,
    'steps': 0.20,
    'fuel': 0.30,   # 增加燃油权重
    'angle': 0.10
}
```

### 场景3：快速避让（优先效率）
```python
weights = {
    'dcpa': 0.25,
    'risk': 0.25,
    'steps': 0.40,  # 增加时间权重
    'fuel': 0.05,
    'angle': 0.05
}
```

## 性能优化建议

如果计算时间过长，可以采用以下策略：

1. **减少搜索范围**
   ```python
   # 仅在关键角度范围搜索
   angle_range=(10, 26)  # 减少角度范围
   ```

2. **分阶段优化**
   ```python
   # 第一阶段：粗搜索（仅前进三）
   coarse_results, _ = optimize_rudder_speed_coordination(
       angle_range=(5, 36, 3),  # 每隔3度
       speed_gears=(0,)         # 仅前进三
   )
   
   # 第二阶段：在promising区域精搜索（所有档位）
   fine_results, _ = optimize_rudder_speed_coordination(
       angle_range=(best_coarse_angle-5, best_coarse_angle+5),
       speed_gears=(0, 1, 2)
   )
   ```

3. **并行计算**
   ```python
   from multiprocessing import Pool
   
   def evaluate_wrapper(args):
       return evaluate_rudder_speed_combination(*args)
   
   with Pool(processes=4) as pool:
       results = pool.map(evaluate_wrapper, parameter_combinations)
   ```

## 扩展方向

1. **增加更多档位**
   - 添加后退档位
   - 更细粒度的速度控制

2. **考虑加速度约束**
   - 速度变化不能太快
   - 避免频繁换挡

3. **多船避让**
   - 同时考虑多艘目标船
   - 更复杂的约束条件

4. **动态调整**
   - 根据实时情况调整策略
   - 在线优化

## 常见问题

### Q1: 为什么选择特定的速度档位？
A: 速度档位基于船舶实际操作规范。前进三(85/60)适合正常航行，前进二(70/60)适合谨慎避让，前进一(48/60)适合紧急减速。

### Q2: 计算时间会增加多少？
A: 理论上增加约3倍（因为多了3个速度档位）。但实际可能更少，因为某些组合会快速被淘汰。

### Q3: 如何验证优化效果？
A: 对比原系统（仅角度优化）和新系统（车舵协同）的最优解，查看DCPA、风险度、时间等指标的改善情况。

### Q4: 权重如何确定？
A: 建议结合领域专家经验和历史数据，通过多次实验确定合适的权重。也可以使用机器学习方法自动学习权重。

## 总结

车舵协同优化相比单纯的角度优化，能够：
- **找到更优的解**：更多的决策自由度
- **更符合实际**：船舶实际操作中需要同时考虑车和舵
- **更灵活**：可以根据场景调整权重

建议先用Demo测试理解逻辑，然后逐步集成到您的完整系统中。
