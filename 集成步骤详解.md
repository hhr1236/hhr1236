# 从Demo到实际应用：车舵协同优化集成步骤

## 概述

本文档指导您如何将demo代码集成到您现有的船舶避让决策系统中。

## 当前状态

✅ **已完成**：
- Demo框架已创建并测试通过
- 多目标优化逻辑已实现
- 可视化功能已验证
- 文档已准备完整

⏳ **待完成**：
- 将demo中的简化仿真替换为您的真实仿真代码
- 根据实际数据调整参数
- 进行实际场景测试

## 集成路线图

### 阶段1：理解Demo（已完成）✅

运行并理解demo的工作原理：
```bash
python rudder_speed_coordination_demo.py
```

查看生成的结果：
- 控制台输出
- `rudder_speed_coordination_result.png`

### 阶段2：准备集成代码

#### 步骤2.1：找到原有代码中的关键位置

在您的原始代码中找到以下部分：

```python
# 原代码位置1：速度设置（需要修改）
list_own_ship_sim = typed.List(list_ship_data()[0])
# 添加：list_own_ship_sim[11] = 速度值

# 原代码位置2：角度评估循环（需要扩展）
for i in range(5, 36):  # 仅角度
    # ... 评估代码 ...
# 修改为：嵌套循环，添加速度维度
```

#### 步骤2.2：创建速度控制函数

在您的代码中添加：

```python
# 添加到您的代码顶部
SPEED_GEARS = {
    0: ('前进三', 85.0 / 60.0),
    1: ('前进二', 70.0 / 60.0),
    2: ('前进一', 48.0 / 60.0),
}

def set_speed_gear(ship_params, gear_idx):
    """
    设置船舶速度档位
    
    参数:
        ship_params: 船舶参数列表（list_ship_data返回的数据）
        gear_idx: 档位索引 (0=前进三, 1=前进二, 2=前进一)
    
    返回:
        修改后的ship_params
    """
    gear_name, gear_value = SPEED_GEARS[gear_idx]
    ship_params_copy = typed.List([v for v in ship_params])
    ship_params_copy[11] = gear_value  # 关键：修改索引11
    return ship_params_copy
```

### 阶段3：修改评估函数

#### 步骤3.1：扩展evaluate_all_maneuvers函数

将原来的单层循环改为双层嵌套循环：

```python
# 原代码：
@jit(nopython=True)
def evaluate_all_maneuvers_with_risk(...):
    results = typed.List()
    
    for i in range(5, 36):  # 仅角度
        # ... 评估单个角度 ...
        results.append((i, status, min_dcpa, max_risk, steps))
    
    return results

# 修改为：
@jit(nopython=True)
def evaluate_all_maneuvers_with_speed(..., speed_gears):
    results = typed.List()
    
    for i in range(5, 36):  # 角度
        for gear_idx in speed_gears:  # 速度档位（新增）
            # 1. 设置速度
            list_own_ship_sim_copy = set_speed_gear(list_own_ship, gear_idx)
            
            # 2. 其余评估逻辑保持不变
            # ... 您原有的评估代码 ...
            
            # 3. 记录结果时增加速度信息
            gear_name, gear_value = SPEED_GEARS[gear_idx]
            results.append((
                i,              # 角度
                gear_idx,       # 速度档位索引（新增）
                gear_value,     # 速度值（新增）
                status,         # 状态
                min_dcpa,       # DCPA
                max_risk,       # 风险度
                steps           # 步数
            ))
    
    return results
```

#### 步骤3.2：更新结果元组定义

```python
# 原定义：
result_tuple_type_risk = numba.types.Tuple((
    numba.int64,    # angle
    numba.int64,    # status
    numba.float64,  # dcpa
    numba.float64,  # max_risk
    numba.int64     # steps
))

# 新定义：
result_tuple_type_speed_risk = numba.types.Tuple((
    numba.int64,    # angle
    numba.int64,    # speed_gear_idx（新增）
    numba.float64,  # speed_value（新增）
    numba.int64,    # status
    numba.float64,  # dcpa
    numba.float64,  # max_risk
    numba.int64     # steps
))
```

### 阶段4：修改帕累托筛选

更新帕累托筛选逻辑以处理新的元组格式：

```python
@jit(nopython=True)
def find_pareto_front_with_speed(evaluation_results):
    """
    更新后的帕累托筛选，考虑速度维度
    """
    successful_solutions = typed.List.empty_list(result_tuple_type_speed_risk)
    for res in evaluation_results:
        if res[3] == 1:  # 注意索引变化：status现在在索引3
            successful_solutions.append(res)
    
    if not successful_solutions:
        return typed.List.empty_list(result_tuple_type_speed_risk)
    
    pareto_front = typed.List.empty_list(result_tuple_type_speed_risk)
    
    for i, sol_A in enumerate(successful_solutions):
        is_dominated = False
        
        for j, sol_B in enumerate(successful_solutions):
            if i == j:
                continue
            
            # 支配关系判断（索引已更新）
            # sol_B[4] = dcpa, sol_B[5] = risk, sol_B[0] = angle, sol_B[6] = steps
            # 添加燃油因子 = sol_B[2] (speed_value)
            
            if (sol_B[4] >= sol_A[4] and      # dcpa更大或相等
                sol_B[5] <= sol_A[5] and      # risk更小或相等
                sol_B[0] <= sol_A[0] and      # angle更小或相等
                sol_B[6] <= sol_A[6] and      # steps更小或相等
                sol_B[2] <= sol_A[2]) and (   # speed更小或相等（燃油）
                sol_B[4] > sol_A[4] or        # 至少一个严格更优
                sol_B[5] < sol_A[5] or
                sol_B[0] < sol_A[0] or
                sol_B[6] < sol_A[6] or
                sol_B[2] < sol_A[2]):
                is_dominated = True
                break
        
        if not is_dominated:
            pareto_front.append(sol_A)
    
    return pareto_front
```

### 阶段5：修改决策选择

更新加权决策函数：

```python
def select_best_from_pareto_with_speed(pareto_front, weights):
    """
    更新后的决策选择，考虑速度/燃油因子
    """
    if not pareto_front:
        return None, None
    
    # 解包数据（注意索引变化）
    angles = np.array([sol[0] for sol in pareto_front])
    speeds = np.array([sol[2] for sol in pareto_front])  # 燃油因子
    dcpas = np.array([sol[4] for sol in pareto_front])
    risks = np.array([sol[5] for sol in pareto_front])
    steps = np.array([sol[6] for sol in pareto_front])
    
    # 归一化
    def normalize(arr):
        min_v, max_v = arr.min(), arr.max()
        return np.ones_like(arr) * 0.5 if max_v == min_v else (arr - min_v) / (max_v - min_v)
    
    norm_angles = normalize(angles)
    norm_speeds = normalize(speeds)  # 新增
    norm_dcpas = normalize(dcpas)
    norm_risks = normalize(risks)
    norm_steps = normalize(steps)
    
    # 计算评分（新增fuel权重）
    scores = (
        weights['dcpa'] * norm_dcpas -
        weights['risk'] * norm_risks -
        weights['steps'] * norm_steps -
        weights['fuel'] * norm_speeds -    # 新增
        weights['angle'] * norm_angles
    )
    
    best_index = np.argmax(scores)
    return pareto_front[best_index], scores[best_index]
```

### 阶段6：更新主程序

```python
if __name__ == '__main__':
    # ... 您的初始化代码 ...
    
    # 定义要评估的速度档位
    speed_gears_to_test = typed.List([0, 1, 2])  # 评估所有三个档位
    
    # 步骤1：评估所有车舵组合
    print("--- (1/4) 正在评估所有候选角度和速度组合... ---")
    all_results = evaluate_all_maneuvers_with_speed(
        'meet', list_own_initial, list_tar_initial,
        list_own_ship_initial_template, deep_dummy,
        p1, p2, RR1, RR2, RISK_THRESHOLD, ORIGINAL_COURSE,
        speed_gears_to_test  # 新增参数
    )
    
    # 步骤2：帕累托前沿筛选
    print("--- (2/4) 正在筛选帕累托最优解集... ---")
    pareto_solutions = find_pareto_front_with_speed(all_results)
    
    # 打印结果（更新格式）
    print("\n--- 帕累托前沿解集 (非支配解): ---")
    print(f"{'角度(°)':<8} | {'速度档位':<12} | {'DCPA(m)':<10} | "
          f"{'最大风险':<12} | {'步数'}")
    print("-" * 60)
    for sol in pareto_solutions:
        gear_name = SPEED_GEARS[sol[1]][0]
        print(f"{sol[0]:<8} | {gear_name:<12} | {sol[4]:<10.1f} | "
              f"{sol[5]:<12.4f} | {sol[6]}")
    
    # 步骤3：加权决策（更新权重）
    print("\n--- (3/4) 正在根据权重选择最终最优解... ---")
    decision_weights = {
        'dcpa': 0.30,
        'risk': 0.30,
        'steps': 0.20,
        'fuel': 0.10,   # 新增
        'angle': 0.10
    }
    
    best_solution, best_score = select_best_from_pareto_with_speed(
        pareto_solutions, decision_weights
    )
    
    # 步骤4：输出最优解（更新格式）
    if best_solution:
        gear_name = SPEED_GEARS[best_solution[1]][0]
        print("\n" + "*" * 60)
        print("--- 最优决策结果 ---")
        print(f"最终推荐的最优转向角度: {best_solution[0]} 度")
        print(f"最终推荐的速度档位: {gear_name} ({best_solution[2]:.3f} m/s)")
        print(f"  - 最小DCPA: {best_solution[4]:.1f} 米")
        print(f"  - 最大风险度: {best_solution[5]:.4f}")
        print(f"  - 避让步数: {best_solution[6]}")
        print("*" * 60)
```

## 测试清单

集成完成后，请进行以下测试：

### 测试1：单速度档位验证
```python
# 仅使用前进三，结果应与原系统接近
speed_gears_to_test = typed.List([0])  # 仅前进三
```
✅ 预期：结果应与原系统相似

### 测试2：双速度档位对比
```python
# 使用前进三和前进二
speed_gears_to_test = typed.List([0, 1])
```
✅ 预期：帕累托前沿应包含两种速度的方案

### 测试3：完整三速度档位
```python
# 使用全部三个档位
speed_gears_to_test = typed.List([0, 1, 2])
```
✅ 预期：找到更多帕累托最优解

### 测试4：不同权重场景
```python
# 紧急避让
weights = {'dcpa': 0.50, 'risk': 0.30, 'steps': 0.10, 'fuel': 0.05, 'angle': 0.05}

# 经济航行
weights = {'dcpa': 0.20, 'risk': 0.20, 'steps': 0.20, 'fuel': 0.30, 'angle': 0.10}
```
✅ 预期：不同权重得到不同的最优解

## 常见问题排查

### 问题1：编译错误
```
TypeError: cannot determine Numba type of ...
```
**解决**：确保所有参数类型正确，特别是新增的speed_gears参数

### 问题2：索引错误
```
IndexError: tuple index out of range
```
**解决**：检查所有访问元组的代码，确保索引已更新

### 问题3：速度未生效
```
# 速度设置后没有影响仿真结果
```
**解决**：
1. 确认`list_own_ship_sim[11]`是正确的速度参数索引
2. 检查是否在每次循环开始时重新设置速度
3. 验证MMG模型是否使用了该参数

### 问题4：计算时间过长
```
# 评估时间是原来的10倍以上
```
**解决**：
1. 减少角度范围或步长
2. 使用并行计算
3. 先用粗搜索，再精搜索

## 性能优化建议

### 优化1：分阶段搜索
```python
# 第一阶段：粗搜索（仅前进三，大步长）
coarse_angles = range(5, 36, 5)  # 每隔5度
speed_gears_coarse = typed.List([0])

# 第二阶段：在promising区域精搜索
fine_angles = range(best_angle-5, best_angle+5)
speed_gears_fine = typed.List([0, 1, 2])
```

### 优化2：并行计算
```python
# 将角度-速度组合分配到多个CPU核心
from multiprocessing import Pool

def evaluate_single_combination(args):
    angle, gear_idx = args
    return evaluate_rudder_speed_combination(angle, gear_idx, ...)

with Pool(processes=4) as pool:
    results = pool.map(evaluate_single_combination, combinations)
```

## 验收标准

集成成功的标志：

✅ 1. 代码能正常编译和运行  
✅ 2. 单速度档位结果与原系统一致  
✅ 3. 多速度档位能找到新的帕累托最优解  
✅ 4. 不同权重能得到合理的不同结果  
✅ 5. 可视化图表正确显示速度信息  
✅ 6. 计算时间在可接受范围内（<10分钟）  

## 后续支持

如遇到问题，请参考：
1. `车舵协同优化_使用指南.md` - 详细技术文档
2. `README_车舵协同优化.md` - 快速参考
3. `rudder_speed_coordination_demo.py` - 完整示例代码

祝集成顺利！🚢
