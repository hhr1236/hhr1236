# Ship Scheduler System (船只调度系统)

一个用于优化船只任务分配和航线规划的智能调度系统。

## 功能特性

### 核心功能
- **船队管理**: 管理多艘船只及其初始位置
- **任务分配**: 自动分配人员运输和物资运输任务
- **路径优化**: 基于距离计算的最优路径选择
- **停靠限制**: 支持最大停靠次数约束
- **优先级管理**: 根据任务紧急程度优化调度

### 优化策略
1. **默认策略 (default)**: 优化总航行距离
2. **最小船只策略 (minimize_ships)**: 优先减少使用的船只数量

### 智能优化
- **零成本任务**: 识别船只已在取货点的任务
- **返程顺路**: 利用返程路线运输物资
- **通用顺路**: 识别途经站点的顺路机会
- **晚间任务**: 基于就近原则分配晚间任务
- **返回任务**: 自动为特定路线添加返回任务

## 安装

### 依赖项
```bash
pip install -r requirements.txt
```

需要的Python包:
- numpy: 用于距离计算
- pandas: 用于成本矩阵管理

### 环境要求
- Python 3.6+
- numpy
- pandas

## 使用方法

### 基本使用
```bash
python ship_scheduler.py
```

程序将依次运行两种策略并输出调度方案。

### 数据配置

#### 1. 船只初始位置 (ship_initial_positions)
```python
ship_initial_positions = {
    "船名": {"platform": (x坐标, y坐标)},
    ...
}
```

#### 2. 人员位置 (personnel_locations)
```python
personnel_locations = {
    "人员名称": "平台代码",
    ...
}
```

#### 3. 物资位置 (item_locations)
```python
item_locations = {
    "物资名称": "平台代码",
    ...
}
```

#### 4. 平台需求 (platform_needs)
```python
platform_needs = {
    "平台代码": {
        "personnel_needed": ["人员1", "人员2"],
        "items_needed": ["物资1", "物资2"],
        "return_to": "返回平台"  # 可选
    },
    ...
}
```

#### 5. 平台坐标 (platform_coordinates)
```python
platform_coordinates = {
    '平台代码': (x坐标, y坐标),
    ...
}
```

#### 6. 任务紧急度 (task_urgency_levels)
```python
task_urgency_levels = {
    "任务类型或目的地": 紧急等级,  # 1=最高, 2=高, 3=普通
    ...
}
```

## 输出说明

### 调度方案输出
```
🚢 【船名】 (日常停靠: X/4, 总停靠: Y)
   - 航行足迹: [平台列表]
   - 任务清单:
     - [类型] 任务描述 (ID: XXX): 从 起点 到 终点 符号
```

### 任务类型标记
- `[主线]`: 主要分配的任务
- `[零成本]`: 船只始于取货点的任务
- `[返程顺路]`: 利用返程路线的任务
- `[通用顺路]`: 途经站点的顺路任务
- `[晚间]`: 晚间分配的任务

### 紧急度符号
- 🔥: 最高优先级 (urgency=1)
- ⚠️: 高优先级 (urgency=2)
- 无符号: 普通优先级 (urgency=3)

## 算法说明

### 调度流程
1. 初始化船只状态
2. 生成人员任务并分配
3. 生成物资任务
4. 前置优化 (零成本、返程顺路、通用顺路)
5. 迭代分配剩余任务
6. 后处理修正 (合并重复路线)
7. 添加返回任务
8. 分配晚间任务

### 成本计算
```
成本 = 距离 × 紧急度系数 + 船只激活成本(可选)

紧急度系数:
- urgency=1: 0.3
- urgency=2: 0.6
- urgency=3: 1.0
```

## 配置参数

```python
MAX_DOCKINGS = 4           # 最大停靠次数
PENALTY_COST = 9999999.0   # 超限惩罚成本
SHIP_ACTIVATION_COST = 100000.0  # 船只激活成本
```

## 优化建议

1. **减少总距离**: 使用 `strategy='default'`
2. **减少船只数量**: 使用 `strategy='minimize_ships'`
3. **提高任务优先级**: 在 `task_urgency_levels` 中设置更低的数值
4. **增加停靠限制**: 调整 `MAX_DOCKINGS` 参数

## 示例场景

程序默认包含一个完整的海上平台船只调度场景:
- 6艘供应船
- 17个海上平台
- 7个人员运输任务
- 14个物资运输任务
- 1个晚间任务

## 输出示例

运行程序后，您将看到:
1. 详细的任务分配过程
2. 每轮决策的成本分析
3. 船只状态更新
4. 优化阶段的处理结果
5. 最终的调度方案总结
6. 两种策略的对比结果

## 许可证

本项目基于开源原则，可自由使用和修改。

## 作者

hhr1236

---

*Ship Scheduler - Intelligent Maritime Logistics Optimization*
